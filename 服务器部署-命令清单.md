# 服务器部署 - 命令清单（按顺序执行）

本清单已根据当前项目检查并修复常见问题，在 **Ubuntu 22.04** 上按顺序执行即可。

---

## 一、SSH 登录

在本机终端执行（替换为你的 IP 和用户名）：

```bash
ssh root@你的服务器IP
```

---

## 二、安装 Docker 与 Docker Compose

在服务器上执行：

```bash
sudo apt update
sudo apt install -y docker.io docker-compose
sudo systemctl start docker
sudo systemctl enable docker
docker --version
docker-compose --version
```

---

## 三、拉取代码

**注意**：仓库克隆后的目录名是 `Enterprise-chat-layout`（首字母大写 E），后面 `cd` 要用这个名称。

```bash
cd /opt
sudo git clone https://github.com/KillerFishAi/Enterprise-chat-layout.git
cd Enterprise-chat-layout
```

---

## 四、配置 JWT 密钥（必做）

生成随机字符串并记住输出：

```bash
openssl rand -base64 32
```

编辑配置：

```bash
nano docker-compose.yml
```

找到：

```yaml
JWT_SECRET: CHANGE_ME_IN_PRODUCTION
```

改为（把下面的 `上面生成的随机字符串` 换成上一步的输出）：

```yaml
JWT_SECRET: 上面生成的随机字符串
```

**不要动** `COOKIE_SECURE: "false"`（用 http 访问时必须为 false）。  
保存：`Ctrl+O` 回车，`Ctrl+X` 退出。  
编辑时**只用空格缩进，不要用 Tab**，避免 YAML 报错。

---

## 五、构建并启动

先清理再启动（避免旧版 docker-compose 的 ContainerConfig 报错）：

```bash
cd /opt/Enterprise-chat-layout
docker-compose down
docker-compose up -d --build
```

若仍报错 `'ContainerConfig'`，可删掉旧镜像再试：

```bash
docker-compose down
docker rmi enterprise-chat-layout_app 2>/dev/null || true
docker-compose up -d --build
```

首次启动时数据库可能尚未就绪，若应用启动失败，等几秒后**再执行一次**：

```bash
docker-compose up -d
```

查看应用日志确认无报错：

```bash
docker-compose logs -f app
```

看到类似 “Ready on port 3000” 即可，按 `Ctrl+C` 退出。

---

## 六、放行 3000 端口

若开启了防火墙：

```bash
sudo ufw allow 3000/tcp
sudo ufw reload
```

云厂商控制台里的「安全组 / 防火墙」也需放行 **3000** 端口。

---

## 七、验证

浏览器访问：

**http://你的服务器IP:3000**

- 能打开登录页即部署成功。
- 点击「立即注册」注册账号，再登录应能进入聊天页。

---

## 八、日常运维命令（在项目目录执行）

| 操作           | 命令 |
|----------------|------|
| 查看应用日志   | `docker-compose logs -f app` |
| 停止所有服务   | `docker-compose down` |
| 再次启动       | `docker-compose up -d` |
| 更新代码后重启 | `git pull` 然后 `docker-compose up -d --build` |

---

## 九、常见问题

- **`git pull` 提示本地修改会被覆盖**  
  执行：`git stash` → `git pull` → `git stash pop`。若有冲突，在 `docker-compose.yml` 里保留你的 `JWT_SECRET` 和 `COOKIE_SECURE: "false"`，删掉冲突标记和错误缩进。

- **YAML 报错 `sequence entries are not allowed here`**  
  检查 `docker-compose.yml` 是否用了 Tab，或有多余/错误缩进。用上面「四」中的格式，**只使用空格**。

- **登录后不跳转**  
  确认 `docker-compose.yml` 里 `app` 的环境变量有 `COOKIE_SECURE: "false"`（用 http 访问时必须为 false）。

- **`docker compose` 报 unknown flag**  
  说明未装 Compose V2，请用带连字符的命令：`docker-compose`。
