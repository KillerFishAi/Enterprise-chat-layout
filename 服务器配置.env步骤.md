# 服务器上配置 .env 的步骤

你在服务器执行了 `npm run env:check` 后，提示缺少 DATABASE_URL、JWT_SECRET、REDIS_URL。按下面做即可。

---

## 第一步：确认 Docker 里数据库和 Redis 已启动

在项目目录执行：

```bash
cd /opt/Enterprise-chat-layout
docker-compose up -d db redis
```

等几秒后执行 `docker-compose ps`，确认 db 和 redis 是 Up。

---

## 第二步：在项目根目录创建 .env 文件

在 **/opt/Enterprise-chat-layout** 下执行下面整段（复制一整块粘贴到终端，回车一次）：

```bash
cat > /opt/Enterprise-chat-layout/.env << 'EOF'
# Next 应用 / Prisma
DATABASE_URL="postgresql://chat_user:chat_pass@localhost:5432/chat_app"
JWT_SECRET="46de94e9a000e5270a8869163ff5f0a4e14d38608bfa66ee0a8042e14151bb1b"
REDIS_URL="redis://localhost:6379"
EOF
```

说明：
- 用户名、密码、库名和 docker-compose.yml 里一致（chat_user / chat_pass / chat_app）。
- 主机用 `localhost` 是因为你在本机跑 Next，连本机的 5432、6379（Docker 已映射）。
- JWT_SECRET 已写死一段随机值，生产环境建议自己换。

---

## 第三步：再检查环境变量

```bash
cd /opt/Enterprise-chat-layout
npm run env:check
```

应看到三个 ✓，最后一行是「环境变量检查通过」。

---

## 第四步：安装依赖、构建、启动

```bash
cd /opt/Enterprise-chat-layout
npm install
npm run build
```

然后按你当前的方式启动：

- **用 Docker 跑 Next**：`docker-compose up -d`（app、ws 也会起来）。
- **在本机跑 Next**：`npm run start`；若单独跑 WebSocket：另开终端 `npm run start:ws`。
- **用 pm2**：例如 `pm2 start npm --name next -- start`，再 `pm2 start npm --name ws -- run start:ws`。

---

## 若不想用命令行创建 .env

1. 用 SFTP、宝塔、VS Code 远程等打开服务器上的 `/opt/Enterprise-chat-layout`。
2. 新建文件，命名为 **.env**（注意前面有个点）。
3. 内容填为：

```env
DATABASE_URL="postgresql://chat_user:chat_pass@localhost:5432/chat_app"
JWT_SECRET="46de94e9a000e5270a8869163ff5f0a4e14d38608bfa66ee0a8042e14151bb1b"
REDIS_URL="redis://localhost:6379"
```

4. 保存后，在项目目录执行第三步、第四步。

---

## 小结

| 步骤 | 命令/操作 |
|------|-----------|
| 1 | `docker-compose up -d db redis` |
| 2 | 用上面的 `cat > .env << 'EOF'` 或手动建 .env 并写三行变量 |
| 3 | `npm run env:check`（应全 ✓） |
| 4 | `npm install` → `npm run build` → 启动 Next 和（如需）WebSocket |
