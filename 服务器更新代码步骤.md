# 服务器更新代码步骤

把服务器上的项目从旧代码更新到最新（已优化）版本，按下面做即可。

---

## 一、前提

- 服务器能访问你的 Git 仓库（GitHub/Gitea 等）。
- 项目目录是你当时 `git clone` 下来的那个路径，例如：`/home/xxx/enterprise-chat-layout` 或 `/var/www/enterprise-chat-layout`。

---

## 二、操作步骤

### 1. 登录服务器

用 SSH 或你平时的方式登录，例如：

```bash
ssh 你的用户名@服务器IP
```

### 2. 进入项目目录

```bash
cd /你的项目路径/enterprise-chat-layout
```

（把 `/你的项目路径/` 换成你实际路径。）

### 3. 拉取最新代码

```bash
git fetch origin
git pull origin main
```

如果仓库主分支不叫 `main` 而是 `master`，把上面第二行改成：

```bash
git pull origin master
```

### 4. 安装依赖（如有新依赖）

```bash
npm install
```

（若用 pnpm：`pnpm install`；yarn：`yarn`。）

### 5. 生成 Prisma 客户端并构建

```bash
npm run build
```

（内部会执行 `prisma generate` 和 `next build`。）

### 6. 重启应用

- **只跑 Next 应用时**：重启你用来跑 `npm run start` 或 `next start` 的进程（如 pm2、systemd、Docker）。
- **单独跑了 WebSocket 服务时**：也要重启一次 `npm run start:ws` 对应的进程。

示例（用 pm2 时）：

```bash
pm2 restart all
# 或只重启指定应用名，例如：
# pm2 restart next-app
# pm2 restart ws-server
```

示例（用 systemd 时）：

```bash
sudo systemctl restart 你的next服务名
sudo systemctl restart 你的ws服务名
```

---

## 三、可选：更新前备份

若担心中间出错，可以先打一个标签再拉取：

```bash
cd /你的项目路径/enterprise-chat-layout
git tag backup-$(date +%Y%m%d-%H%M)
git pull origin main
```

这样万一要回退，可以用 `git checkout backup-xxxxxx` 回到拉取前的状态。

---

## 四、更新后建议检查

1. **环境变量**（必填的没变）  
   ```bash
   npm run env:check
   ```  
   若单独跑 WebSocket，再执行：  
   ```bash
   npm run env:check:ws
   ```

2. **访问网站**  
   浏览器打开你的域名或 IP，确认登录、发消息、实时消息是否正常。

3. **WebSocket**  
   若用了 `start:ws`，确认 3001（或你设的 `SOCKET_PORT`）端口正常，且实时消息能收到。

---

## 五、命令汇总（复制一整段执行）

把下面两处的「你的项目路径」改成实际路径后，可以整段复制到服务器执行：

```bash
cd /你的项目路径/enterprise-chat-layout
git fetch origin
git pull origin main
npm install
npm run build
# 然后用你当前的方式重启 Next 和 WebSocket，例如：
# pm2 restart all
```

按上面做完，服务器上的代码就会更新到你优化后的最新版本。
