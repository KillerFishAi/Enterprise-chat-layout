# 把代码上传到仓库（一步步跟着做）

按下面顺序做，就能把项目推到 GitHub 或 Gitee，服务器再用 `git clone` 拉下来即可。

---

## 第一步：在电脑上安装 Git（若还没装）

1. 打开：https://git-scm.com/download/win  
2. 下载并安装，一路「下一步」即可。  
3. 安装完成后，在 Cursor 里打开**终端**（或 PowerShell），输入：
   ```bash
   git --version
   ```
   能输出版本号就说明装好了。

---

## 第二步：创建一个空仓库（选一个平台）

### 用 GitHub

1. 打开 https://github.com ，登录账号。  
2. 右上角点 **+** → **New repository**。  
3. **Repository name** 填：`enterprise-chat-layout`（或任意英文名）。  
4. 选 **Public**，**不要**勾选 "Add a README file"。  
5. 点 **Create repository**。  
6. 创建好后，页面上会有一个地址，类似：
   ```text
   https://github.com/你的用户名/enterprise-chat-layout.git
   ```
   复制这个地址，后面要用。

### 用 Gitee（国内访问更快）

1. 打开 https://gitee.com ，登录账号。  
2. 右上角 **+** → **新建仓库**。  
3. **仓库名称**填：`enterprise-chat-layout`。  
4. 选 **公开**，**不要**勾选「使用 Readme 文件初始化仓库」。  
5. 点 **创建**。  
6. 创建好后，页面上有**克隆地址**，选 **HTTPS**，类似：
   ```text
   https://gitee.com/你的用户名/enterprise-chat-layout.git
   ```
   复制这个地址，后面要用。

---

## 第三步：在项目文件夹里用 Git 上传

在 Cursor 里打开终端，**确保当前在项目目录**（路径里有 `enterprise-chat-layout`）。  
若不在，先执行：

```bash
cd D:\下载\enterprise-chat-layout
```

然后**依次**执行下面每一条命令（把 `你的仓库地址` 换成第二步复制的地址）：

```bash
git init
```

```bash
git add .
```

```bash
git commit -m "首次提交"
```

```bash
git branch -M main
```

```bash
git remote add origin 你的仓库地址
```

例如仓库地址是 `https://github.com/zhangsan/enterprise-chat-layout.git`，就执行：

```bash
git remote add origin https://github.com/zhangsan/enterprise-chat-layout.git
```

最后推送：

```bash
git push -u origin main
```

- 若用 **GitHub**：可能会提示你打开浏览器登录或输入账号密码（或 Token）。  
- 若用 **Gitee**：按提示登录即可。

推送成功后，在网页上刷新仓库，就能看到所有代码（没有 `node_modules` 和 `.next`，因为已被 `.gitignore` 忽略）。

---

## 第四步：在服务器上拉取代码

SSH 登录到你的 VPS 后，执行（把地址换成你自己的仓库地址）：

```bash
cd /opt
sudo git clone https://github.com/你的用户名/enterprise-chat-layout.git
cd enterprise-chat-layout
```

然后按 **VPS下一步-2C4G-Ubuntu.md** 里的「第四步」改 JWT 密钥，再执行「第五步」构建并启动即可。

---

## 常见问题

**Q：`git push` 时提示要登录？**  
- GitHub：现在一般要用 **Personal Access Token** 当密码，在 GitHub 网页 → Settings → Developer settings → Personal access tokens 里生成。  
- Gitee：用账号密码或按页面提示绑定本机 Git。

**Q：提示 "failed to push" 或 "rejected"？**  
- 若仓库网页上勾选过「用 README 初始化」，需要先执行：
  ```bash
  git pull origin main --rebase
  git push -u origin main
  ```

**Q：以后改了代码，怎么再上传？**  
- 在项目目录执行：
  ```bash
  git add .
  git commit -m "说明你改了什么"
  git push
  ```
  服务器上到项目目录执行 `git pull` 即可更新。
