# 直接部署到服务器（新手详细版）

本方案**不涉及本地运行**，只在服务器上部署应用和数据库。按下面顺序一步一步做即可。

---

## 部署前你需要准备

1. **一台服务器**：能 SSH 登录（Linux 推荐），有公网 IP 或内网可访问。
2. **服务器上已安装**：
   - **Docker** 和 **Docker Compose**（推荐，见下方「方式一」）；  
   或
   - **Node.js 18+**、**npm**，以及 **PostgreSQL**（见「方式二」）。
3. **代码在 Git 仓库里**：例如 GitHub / Gitee，服务器能 `git clone` 到该仓库。

---

# 方式一：Docker Compose 部署（推荐）

**特点**：一条命令同时启动数据库和应用，无需在服务器上单独装 PostgreSQL 和 Node。

---

## 第一步：登录服务器

在你自己电脑上打开终端（PowerShell 或 CMD），用 SSH 登录服务器，例如：

```bash
ssh root@你的服务器IP
```

（把 `你的服务器IP` 换成实际 IP；用户名若不是 `root` 就改成你的用户名。）  
输入密码或使用密钥登录，进入服务器后继续下面步骤。

---

## 第二步：安装 Docker 和 Docker Compose（若尚未安装）

在服务器上执行：

```bash
# 若为 CentOS / RHEL
sudo yum install -y docker
sudo systemctl start docker
sudo systemctl enable docker

# 若为 Ubuntu / Debian
sudo apt update
sudo apt install -y docker.io docker-compose
sudo systemctl start docker
sudo systemctl enable docker
```

安装完成后可检查：

```bash
docker --version
docker-compose --version
```

两个命令都能输出版本号即可。

---

## 第三步：把代码拉到服务器

在服务器上选一个目录（例如 `/opt` 或你的家目录），执行：

```bash
cd /opt
git clone https://你的仓库地址.git enterprise-chat-layout
cd enterprise-chat-layout
```

（把 `https://你的仓库地址.git` 换成你真实的 Git 地址；若没有 Git 仓库，需要先把本地项目推到 GitHub/Gitee 再在服务器上 clone。）

若项目已存在，只需更新代码：

```bash
cd /opt/enterprise-chat-layout
git pull
```

---

## 第四步：修改生产环境密钥（必做）

1. 在服务器上进入项目目录：
   ```bash
   cd /opt/enterprise-chat-layout
   ```
2. 生成随机字符串（可选，用于 JWT_SECRET）：
   ```bash
   openssl rand -base64 32
   ```
   复制输出的那一行，后面要用。
3. 编辑 `docker-compose.yml`：
   ```bash
   vi docker-compose.yml
   ```
   或使用 `nano docker-compose.yml`（若已安装 nano）。
4. 找到这一行：
   ```yaml
   JWT_SECRET: CHANGE_ME_IN_PRODUCTION
   ```
   改成（把「你刚才复制的随机字符串」换成第三步生成的，或自己写一长串英文/数字）：
   ```yaml
   JWT_SECRET: 你刚才复制的随机字符串
   ```
5. 保存并退出（vi：按 `Esc` 输入 `:wq` 回车；nano：`Ctrl+O` 回车，`Ctrl+X`）。

**说明**：`DATABASE_URL` 在 `docker-compose.yml` 里已写好，无需改；数据库账号密码为 `chat_user` / `chat_pass`，仅限本机访问，可按需自行修改同文件中的 `POSTGRES_USER`、`POSTGRES_PASSWORD` 和 `app` 的 `DATABASE_URL`。

---

## 第五步：构建并启动

仍在项目目录下执行：

```bash
docker-compose up -d --build
```

- `-d` 表示后台运行；`--build` 表示先构建镜像再启动。
- 会先启动 PostgreSQL，健康检查通过后再启动应用；应用启动时会自动执行 `npx prisma migrate deploy` 建表。
- 完成后应用监听 **3000** 端口。

若需查看启动日志：

```bash
docker-compose logs -f app
```

看到无报错、有 Next.js 或监听 3000 的日志即可。按 `Ctrl+C` 退出日志。

---

## 第六步：验证访问

1. **本机或同网访问**：浏览器打开 **http://服务器IP:3000**  
   若服务器有防火墙，需放行 3000 端口，例如：
   ```bash
   # CentOS / RHEL (firewalld)
   sudo firewall-cmd --permanent --add-port=3000/tcp
   sudo firewall-cmd --reload

   # Ubuntu (ufw)
   sudo ufw allow 3000/tcp
   sudo ufw reload
   ```
2. 能打开**登录页**即表示部署成功。
3. 点击「立即注册」，注册一个账号后再登录，进入聊天主界面即可使用。

---

## 常用运维命令（Docker Compose）

在项目目录 `/opt/enterprise-chat-layout` 下执行：

| 操作       | 命令 |
|------------|------|
| 查看应用日志 | `docker-compose logs -f app` |
| 停止所有服务 | `docker-compose down` |
| 再次启动     | `docker-compose up -d` |
| 仅重启应用   | `docker-compose restart app` |
| 更新代码后重启 | `git pull` 然后 `docker-compose up -d --build` |

---

# 方式二：不用 Docker（Node + PM2 + 本机 PostgreSQL）

**适合**：服务器上已经装了 Node.js 和 PostgreSQL，不想用 Docker。

---

## 第一步：在服务器上安装 Node.js 和 PostgreSQL

- **Node.js**：建议 18 或 20 LTS，可从 [nodejs.org](https://nodejs.org/) 或使用 `nvm` 安装。
- **PostgreSQL**：建议 14 或 16；安装后创建数据库和用户，例如：
  ```sql
  CREATE USER chat_user WITH PASSWORD '你的密码';
  CREATE DATABASE chat_app OWNER chat_user;
  ```
  记下：**用户名**、**密码**、**数据库名**。

---

## 第二步：拉取代码并安装依赖

```bash
cd /opt
git clone https://你的仓库地址.git enterprise-chat-layout
cd enterprise-chat-layout
npm install
```

---

## 第三步：配置环境变量

在项目根目录创建 `.env` 文件（不要提交到 Git）：

```bash
cd /opt/enterprise-chat-layout
vi .env
```

写入以下内容（把用户名、密码、数据库名换成你在第一步创建的）：

```env
DATABASE_URL=postgresql://chat_user:你的密码@localhost:5432/chat_app
JWT_SECRET=请用随机长字符串
```

`JWT_SECRET` 可用 `openssl rand -base64 32` 生成后粘贴进去。保存退出。

---

## 第四步：执行数据库迁移并构建

```bash
npx prisma migrate deploy
npm run build
```

---

## 第五步：用 PM2 启动并设置开机自启

```bash
pm2 start npm --name chat-app -- start
pm2 save
pm2 startup
```

按 `pm2 startup` 输出的提示执行最后那一条命令（通常是 `sudo env PATH=...`）。  
之后访问 **http://服务器IP:3000** 验证，步骤同方式一第六步。

---

# 小结：直接部署到服务器你主要做哪些事

| 步骤 | 方式一（Docker） | 方式二（Node+PM2） |
|------|------------------|---------------------|
| 1 | 登录服务器，安装 Docker + Docker Compose | 登录服务器，安装 Node.js + PostgreSQL |
| 2 | `git clone` 项目，`cd` 进项目目录 | 同上 |
| 3 | 编辑 `docker-compose.yml`，改 `JWT_SECRET` | 创建 `.env`，填 `DATABASE_URL` 和 `JWT_SECRET` |
| 4 | `docker-compose up -d --build` | `npx prisma migrate deploy`，`npm run build`，`pm2 start npm --name chat-app -- start` |
| 5 | 浏览器访问 http://服务器IP:3000，注册并登录 | 同上 |

遇到某一步报错，把终端里的**完整报错**复制下来，便于排查。
